# kvartiri.bg


In order to run the project, you need 2 separate terminals. In the first terminal you need to run "python3 manage.py tailwind start"- this will start the tailwind server, which is required for the UI. In the second terminal, you need to run "python3 manage.py runserver".
